<Application x:Class="TiaOpenness.Gui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    <SolidColorBrush x:Key="Ink" Color="#1F2933" />
    <SolidColorBrush x:Key="Muted" Color="#6B7A8C" />
    <SolidColorBrush x:Key="Line" Color="#D8DEE6" />
    <SolidColorBrush x:Key="Surface" Color="#FFFFFF" />
    <SolidColorBrush x:Key="Canvas" Color="#F4F6F8" />
    <SolidColorBrush x:Key="Accent" Color="#0F6FC5" />
    <SolidColorBrush x:Key="Danger" Color="#C0392B" />
    <SolidColorBrush x:Key="Warn" Color="#B5730B" />
    <SolidColorBrush x:Key="Good" Color="#1E7B4D" />

    <Style TargetType="Button">
      <Setter Property="Padding" Value="12,6" />
      <Setter Property="Margin" Value="0,0,6,0" />
      <Setter Property="MinWidth" Value="86" />
    </Style>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource Ink}" />
    </Style>
    <Style x:Key="Section" TargetType="TextBlock">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Margin" Value="0,0,0,6" />
      <Setter Property="Foreground" Value="{StaticResource Muted}" />
      <Setter Property="FontSize" Value="11" />
    </Style>
  </Application.Resources>
</Application>
